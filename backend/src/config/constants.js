/** Must match `InteractionKind` in the frontend. */
export const INTERACTION_KINDS = [
  "blog",
  "repository",
  "portfolioWork",
  "certification",
  "experience",
];

export const KIND_SET = new Set(INTERACTION_KINDS);
