# Portfolio API (Express + MongoDB) — set to the same host:port as your Express server.
# Required for `next build` / production. Dev fallback is http://localhost:5001 if omitted.
NEXT_PUBLIC_API_URL=http://localhost:5001
